---
name: db-migrate
description: Generate, review and dry-run database migrations for this stack (Prisma + Postgres), including a rollback file and a data-backfill checklist. Use when the user mentions "migration", "schema change", "add a column", "alter table", or asks whether a change is backwards compatible.
---

# db-migrate

Procedure body.
